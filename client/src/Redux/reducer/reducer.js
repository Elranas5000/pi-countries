//primero importamos las actions-types:

//definimos el initialState:
let initialState = {
    countries : [],
    activity:[],
}

//definimos la funcion rootReducer:
function rootReducer(state = initialState, action){

}

export default rootReducer;